============
Installation
============

At the command line::

    $ easy_install insupportable

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv insupportable
    $ pip install insupportable
